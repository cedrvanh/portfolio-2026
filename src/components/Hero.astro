---
---

<section class="hero" aria-label="Introduction">
  <h1 class="headline reveal" style="--delay: 1">
    I'm a <em>full stack developer</em> with a good eye for UX/UI.
  </h1>
  <p class="sub reveal" style="--delay: 2">
    Hi there! I'm Cedric, a full stack developer who's passionate about building web applications that are both functional and look good. I also love keeping up with new technologies and always look for opportunities to learn.
  </p>
</section>

<style lang="scss">
  .hero {
    margin-top: clamp(4rem, 14vh, 8rem);
  }

  .headline {
    font-family: var(--font-display);
    font-weight: 400;
    font-size: clamp(2.6rem, 6.5vw, 4rem);
    line-height: 1.14;
    letter-spacing: -0.01em;
    text-wrap: balance;

    em {
      font-style: italic;
      color: var(--accent);
    }
  }

  .sub {
    margin-top: 1.75rem;
    max-width: 36rem;
    color: var(--muted);
    font-size: 1.1rem;
  }
</style>
